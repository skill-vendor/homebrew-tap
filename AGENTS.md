# Homebrew Tap for skv

Homebrew formula for [skill-vendor/skv](https://github.com/skill-vendor/skv).

## Structure

- `Formula/skv.rb` - Homebrew formula (macOS/Linux, ARM/Intel)

## Releasing

Update `version` and `sha256` checksums in the formula when releasing new versions.
